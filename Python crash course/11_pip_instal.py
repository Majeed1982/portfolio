# installing pip
# write in terminal or cmd python -m pip install --upgrade pip
#  python -m pip, tells Python to run the module pip. The second part, 
# install --upgrade, tells pip to update a package that’s already been installed.

# Installing pytest
# write in terminal or cmd python -m pip install --user pytest

